0a. Implement MarkupSpecifier. Add markup to [A].

0b. Reconfigure running figure maker slurs.

1. Hook custom WellformednessManager into SegmentMaker.

2. Add attachment test to Slur to complain if attached to only a single leaf.
Do the same for Beam, Glissando, Hairpin, OctavationSpanner (?), PhrasingSlur,
PianoPedalSpanner (?), TextSpanner (?), Tie and TrillSpanner.

3. Extend FigureMaker with generalized 'specifiers' property.

4. Collapse ArticulationFigureSpecifier, ArticulationHandler,
PatternedArticulationHandler, ReiteratedArticulationHandler into a single
ArticulationSpecifier class.

5. Check for duplicate figure names.

6. Build a fermata figure maker.

7. Write SegmentMaker tests for final_barline, final_markup,
final_markup_extra_offset, label_clock_time, print_segment_duration,
score_template, skips_instead_of_rests, tempo_specifier, transpose_score,
volta_specifier.