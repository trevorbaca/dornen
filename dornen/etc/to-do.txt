1. Implement preferred denominator as a StageRhythmMaker property together
with optional calltime override. Update API example time signatures.

2. Containerize FigureMaker output to allow slurs across stages with
Selector.with_next_leaf(). Add slur across stages in running figure maker.

3. Add notational arrays to the docstrings of each Dornen figure maker.

4. Make figure maker that produces Huitzil-style tuplet monads.

5. Deposit mock-up figures in [A] - [G].

6. Implement a better RegisterTransitionSpecifier. Possibly named
RegisterInterpolationSpecifier. Allow for interpolation by duration. Allow for
interpolation by number of attacks.

7. Implement OctaveDisplacementSpecifier in [A].

8. Create a revelation passage in [A]. Use cleanly implemented cursor in
segment definition file.